/**
 * @deprecated Import from supabaseBackupService instead.
 * Kept for backward compatibility during migration.
 */
export {
    supabaseBackupService as postgresqlService,
    supabaseKeyBackupService as keyBasedService,
} from './supabaseBackupService';